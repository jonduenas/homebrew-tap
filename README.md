# Homebrew Tap for Jon Duenas's Tools

This is a custom Homebrew tap for various development tools.

## Installation

```bash
brew tap jonduenas/tap
brew install xcbuild-parser
```

## Available Formulae

- **xcbuild-parser** - Parse xcodebuild output and convert to structured JSON
